# Needs python path
refs:
    - python.yml

config:
    # Output build directory
    outputDir: ${PROJECTDIR}/out

tasks:
    # Clean task
    clean:
        description: Clean build output
        emoji: broom
        builder: nmk_base.builders.CleanBuilder
        params:
            path: ${outputDir}
